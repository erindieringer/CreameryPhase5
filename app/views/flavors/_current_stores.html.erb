<h3>Current Stores</h3>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @flavor.store_flavors.each do |store_flavor| %>
      <tr>
        <td><%= link_to store_flavor.store.name, store_path(store_flavor.store) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
